# Update README with mention of calendar sync for 2Brain
# Append at the end

## Calendar Sync

Run the nightly workflow *Calendar Sync (SecondBrain)* to automatically generate an `ics` file from any note that contains a `date:` field.  The file can be downloaded as an artifact and imported into Google Calendar, Apple Calendar etc.
